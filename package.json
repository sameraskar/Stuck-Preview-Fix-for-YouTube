{
  "name": "stuck-preview-fix",
  "version": "1.0.0",
  "private": true,
  "description": "Open-source MV3 fix for stuck YouTube inline previews after new-tab actions.",
  "license": "MIT",
  "scripts": {
    "build": "python scripts/build.py",
    "validate": "python scripts/validate.py"
  }
}
